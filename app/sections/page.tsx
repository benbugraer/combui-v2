import type {Metadata} from "next";

export const metadata: Metadata = {
    title: "CombUI | sections",
    description:
        "sections for your projects.",
};

export default function SectionsPage() {
    return <div>Sections Page</div>;
}
