import type {Metadata} from "next";

export const metadata: Metadata = {
    title: "CombUI | Components",
    description:
        "Select any component and only copy paste",
};


export default function ComponentsPage() {
    return <div>Components Page</div>;
}
